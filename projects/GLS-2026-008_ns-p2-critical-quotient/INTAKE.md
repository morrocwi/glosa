# GLS-2026-008 — P02 intake: NS P2 Standalone (Critical Quotient Closure, T1–T3)

tier: Dr (specified; self-labeled OPEN throughout by its own source document; unreviewed here)

## Two-question intake (P02)

**Q1 — what is the retained difference this issue names?**
A 3599-line research-program document exists under two different filenames on disk
(`NS_P2_STANDALONE.md` and `NS_P2_STANDALONE_CRITICAL_QUOTIENT_T1_T3_vNext.md`), confirmed
byte-identical by diff — the retained difference is that this is genuinely ONE document, not two,
and it is currently unregistered anywhere in this workspace. The document itself is a direct
attempt at the continuum 3D Navier–Stokes regularity problem (a "P2 critical-normalized
finite-observation escape contraction" target, decomposed into three theorem obligations T1
compactness, T2 local saturation rigidity, T3 no-infinite-zero-defect-genealogy), using classical
harmonic-analysis/PDE technique — not this workspace's own discrete/readout framework, unlike
GLS-2026-006/007. Its own status ledger (Section 30) marks most headline results OPEN, several
REFUTED (two previously-attempted T2 shortcuts), a few DERIVED (mostly algebraic identities and
one finite-dimensional linear-algebra fact), and none of the three main theorems (T1/T2/T3) or
the overall P2/Clay targets closed. A later "Canonical Architecture Upgrade" section (46 onward)
reformulates the whole program in different notation (Retain→Refine→Trace / Q→L→A,
target `K_∞ = ∅`) and states it supersedes the earlier T1/T2/T3 framing — this reformulation was
read but not yet reconciled against the earlier sections' own status ledger.

**Q2 — who is affected versus who holds power?**
Same as GLS-2026-006/007: the founder is sole author and holds full release/registration
authority. No third party's interests are affected by recording this intake. This document makes
no attribution to, or claim against, any external party — it is fully self-contained work.

`zero_vs_bottom`: NOT yet checked — no Toledo lookup has been performed for anything in this
document (see `scope_decision_disclosed` in `blackbox_note.yaml`). One genuine connection point
is flagged but unverified: Section 3's domain-weld/quotient-gate equation
(`q_{j+1}∘F_NSE = F#_j∘q_j`) uses the same schema already canonical in this workspace at
`weld/M.02.v1` (CAN-006) — whether this is the SAME object, a genuine instance, or coincidental
notational overlap has not been determined.

## What this intake does

1. Records the document's existence, confirms the two on-disk filenames are byte-identical
   (treated as one document), and records its self-declared claim boundary and status ledger.
2. Discloses honestly that only ~80% of the document (through line 2874 of 3599) has been read.
3. Flags the document's relationship to, and difference in kind from, `GLS-2026-006`/`GLS-2026-007`
   (this workspace's own discrete/readout-native NS work) — this is a classical-PDE attack on the
   continuum problem itself, not a readout-layer question about a continuum result.
4. Flags one unverified possible connection point to an already-canonical Toledo schema
   (`weld/M.02.v1`) for a future Toledo-first check, without performing that check here.
5. Registers this as `GLS-2026-008` in `registry/RESEARCH_REGISTRY.yaml`, `spine_stage: intake`.

## What this intake explicitly does NOT do

- Does not read the unread tail of the document (lines 2875–3599).
- Does not run any Toledo lookup, duplication check, or registration for anything in this
  document, including the one flagged possible connection point.
- Does not run the full glosa spine (P02 only, matching this session's established precedent for
  a "record first" instruction).
- Does not draft any paper, push any repo, run any Coq work, or touch Zenodo.
- Does not adjudicate between the document's own earlier T1/T2/T3 framing and its later
  Retain→Refine→Trace reformulation — both are recorded as read, neither is evaluated here.
- Makes no claim, and asserts no view, on whether any part of this document constitutes progress
  toward the Clay Millennium Navier–Stokes problem — the document's own claim discipline (Section
  0) states plainly that it does not, and this intake does not second-guess that boundary.
