# Candidate hypotheses — GLS-2026-003 (master-equation-river)

> tier: Dr (specified; independently unreviewed). One candidate, lens-out from
> `lens_translation.md`, in world/discipline language. **AI drafts candidates; AI never selects.**
> Selection of H1 (yes/no) is the founder's own act, recorded in `hypothesis_selection.yaml` — not
> performed in this file. Carries `claim_type` per `schema/claim_card.schema.json`'s vocabulary
> (`CONCEPTUAL`/`INTERPRETIVE` is not always the literal enum value — see the claim card's own
> `claim_type_mapping_note` for the schema-legal mapping, the same convention GLS-2026-002 used).

---

## H1 — Master Equation River as a typed, provenance-honest dependency closure

**id:** H1
**claim_type (world language):** INTERPRETIVE / provenance-and-composition claim

**statement (EN):** The programme's ten prior 2026 manuscripts (Readout Genesis, Human Learning as
Epistemic Architecture, Experience Is the Human LoRA, RG-MEMK, From Problem to Hypothesis, Epistemic
Fusion or Tunnel v8.1, Meaning Before Naming, Experience Is Meaning-Giving v2, Effective/Corrigible
Agency Potential, Choice Begins Before Choice, CTSA Human-Return Readout) describe successive stages
of one process (World -> Readout -> Meaning -> Experience -> Retention -> Accessibility -> Live
Possibility -> Choice -> Agency -> Feedback -> Changed Reader -> Human-AI -> Human Return -> Warrant)
and can be composed as a typed multi-level dependency closure, per the Master Equation River
provenance audit (`Master_Equation_River_Provenance_Audit_v1_0.pdf`, 5 September 2026). With retained
change (a reader's state $H_{t+1} \neq H_t$ persists across an encounter), the closure is a recursive
river or spiral, not a closed circle — the audit's own one-sentence summary. This project checks that
claim against the ten named manuscripts fetched directly from their own Zenodo records, not against
the audit's own summary of them.

**statement (TH):** ต้นฉบับสิบชิ้นของโปรแกรม (2026) อธิบายคนละช่วงของกระบวนการเดียวกันตามที่ audit
เสนอ (World -> Readout -> Meaning -> Experience -> Retention -> Accessibility -> Live Possibility ->
Choice -> Agency -> Feedback -> Changed Reader -> Human-AI -> Human Return -> Warrant) และประกอบกัน
เป็น typed multi-level dependency closure ได้ เมื่อมี retained change ($H_{t+1} \neq H_t$) วงจรจึงเป็น
recursive river/spiral ไม่ใช่วงกลมปิด -- โครงการนี้ตรวจข้อเสนอนั้นเทียบกับต้นฉบับสิบชิ้นที่ดึงมาตรง
จาก Zenodo เอง ไม่ใช่เทียบกับบทสรุปของ audit เอง

**falsifier:** A stage whose attributed internal source, when fetched directly from its own Zenodo
record and read in full, does not in fact contain the content the audit's genealogy table or prose
attributes to it (a content-absence finding); OR two stages that can only be composed into the
Master equation (eq. 42 of the audit) by collapsing a non-collapse the audit itself declares (e.g.
treating accessibility as warrant, resonance as truth, retention as improvement, or potential as
performance).

**what evidence would separate it:** The literature pass already run
(`records/lit/master-equation-river/h1/`): ten citation cards, one per named source, each fetched
directly from Zenodo (10.5281/zenodo.<id>) and converted to full text, each carrying one continuous
verbatim passage checked against the audit's own attributed content for that stage, with a
YES/NO/ORTHOGONAL/UNDETERMINED stance per source. `litreview_manifest.yaml`'s accuracy and diversity
gates both report PASS.

**Hypothesis derived with Readout Universe — Yaoharee Lahtee (lens, DOI 10.5281/zenodo.21529456);
candidate proposed by AI, selection reserved to the founder.**

---

## Selection status

**Not selected in this file.** Per glosa gate rule 5, selection of H1 (the project's only candidate)
is reserved to the founder and is recorded in `hypothesis_selection.yaml`.
