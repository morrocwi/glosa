# C0_SCHEMA — first sound outer abstraction for the Readout–Clay occupation primal/dual (round 0)
tier: Dr (specification). Founder request CTX ~15:40 ("เขียน C0_SCHEMA ให้ precise ถึงระดับชื่อ vector indices และสมการแต่ละ row").
Instantiated for the W3 lane on the count/load ledger; all rows are linear so (P_r) is an exact rational LP.
Rule: every row must carry an inclusion certificate (exact events satisfy it) before it is used; rows marked [OUTER]
are deliberately weak, rows marked [EXACT] are identities of the registered stepper, rows marked [FIXTURE] are proven
on the named fixture only and are NOT yet admissible as cuts.

## 0. Vertex types (strata × retained class)
  v ∈ V = V_I ⊔ V_L ⊔ V_U.  Round 0 uses V_I = {SEED, IN}, V_U = {DIFF}, V_L = {FAR}; EXIT is a sink (not a vertex).
  (SEED = addresses of the frozen datum after joint reanchor; IN = in-cell descendants |t|² < 3 of the current cell;
   DIFF = load below the declared resolution threshold τ_res of the reader inside the cell; FAR = addresses with
   ancestry depth ≥ M_* below the current cell.)

## 1. Event vector z_e ∈ ℚ^{n_e}, n_e = 14 (indices fixed)
  0  S⁻      retained critical load in (≥0)          1  S⁺      retained critical load out (≥0)
  2  A⁻      ancestry burden in (≥0)                  3  A⁺      ancestry burden out (≥0)
  4  Gsrc    total source activity at targets (≥0)    5  Gnet    net retained effect (≥0)
  6  Gact    births kept active (≥0)                  7  Ccan    cancellation amount (≥0)   [NOT a defect]
  8  R       certified resolutions → terminal (≥0)    9  X       registered exit load (≥0)
  10 D       payable defect (≥0)                      11 Bk      backscatter/return into v (≥0)
  12 L       registered relaxation loss on invariants (≥0)
  13 p       productivity scalar (≥0)
  (Ω chart data and μ motif distribution are NOT coordinates in round 0: they are absorbed by the vertex type;
   phase coordinates are dropped = outer relaxation. I is represented by row I0 only.)

## 2. Rows of C_e^(0) (all e)
  ACC0 [EXACT]   Gsrc = Gnet + Ccan                                  (ledger identity: source = net + cancelled)
  ACC1 [EXACT]   Gnet = Gact + Bk + R + X + D   (net effect lands as: births kept active, feedback/return onto
                 existing addresses, certified resolution, exit, or defect — no fifth channel; M1 checker verifies it)
  S0   [OUTER]   S⁺ ≤ S⁻ + Gact + Bk                                 (no δ; no contraction encoded)
  A0   [OUTER]   A⁺ ≤ A⁻ + Gact + Bk                                 (ρ_A = 1)
  I0   [EXACT]   S⁺ + L = S⁻ + Gact + Bk − X   on the zero-defect face (energy-type invariant with declared loss L)
  ZX   [FACE]    D = 0,  X = 0
  P0   [OUTER]   p ≤ Gsrc                                            (until Witness Soundness certifies p = future-relevant part)
  NN            all 14 coordinates ≥ 0
  Inclusion certificate for round 0: ACC0/ACC1/I0 are identities of the sparse stepper's ledger (rg_run1_planar.py /
  rg_t0_02_stepper.py energy identity ≤ 1e-10); S0/A0/P0 are implied by I0 and NN. Closedness: all rows are closed
  half-spaces/hyperplanes ⇒ C^(0) closed (needed by RC-OCC-SOUND's Cesàro step).

## 3. Balance matrix B (retained coordinates r ∈ {S, A})
  (Bz)_{v,S} = Σ_{e: h(e)=v} S⁺_e − Σ_{e: t(e)=v} S⁻_e ;   (Bz)_{v,A} likewise with A.
  BAL: Bz = 0 for every v ∈ V (stationary occupation).   NORM: Σ_e p_e = 1.

## 4. Round-0 event catalogue (W3 lane) and what is known exactly
  E_S  : SEED→{SEED, IN, EXIT}: per unit S⁻=1: feedback 14, births 24 (IN), exits 30 (counts from rg_m2_targets.json;
         load-weighted version to be filled from the T0-02 run) — [EXACT counts]; on the ZX face X = 0 forces z_{E_S} = 0.
  E_IN : IN→{IN, SEED, EXIT}: gen-2, UNKNOWN — round 0 leaves (a,b,c) free ≥ 0  [OUTER by construction].
  E_DU, E_FL : DIFF and FAR steps: round 0 leaves them free (outer).
  Result (rg_occ_c0_w3.json): P_0 feasible, supp = {E_IN self-loop}; classification NO_CLOSURE (missing edge/type
  content, not a missing inequality) ⇒ required action per §13 of the founder's text: ADD EVENTS, i.e. the exact gen-2
  event catalogue from the populated T0-02 run (cut H_gen2), not an ad-hoc inequality.

## 5. Round protocol (fixed)
  r → solve (P_r) exactly (Fractions simplex or sympy linsolve on the small system) →
  UNSAT: extract Farkas (y, η, λ ≥ 0), write rg_occ_cert_r.json, independent checker expands Bᵀy − η p − Σλ·rows ≡ 0 →
  DUAL_CERTIFIED_r (conditional on inclusion certificates of all rows used).
  SAT: record supp(z_r), minimal-support circulation 𝒪_r (min |supp| by enumeration on the small event set), classify
  {MISTRANSLATION, LOST_INFORMATION, NO_CLOSURE, INSUFFICIENT_RESOLUTION, CALIBRATION_FAILURE}; LOST_INFORMATION ⇒ add
  coordinate; NO_CLOSURE ⇒ add event/type; otherwise a cut H_r with its inclusion proof. No cut without proof.
  Terminal: DUAL_CERTIFIED / EXACT_PRIMAL_REALIZED (needs trajectory realization Q_{e_n} z_n = P_{e_{n+1}} z_{n+1}) / UNRESOLVED.
  Layer order after H_gen2: W1, W2, W3 (rank-2 targets ⇒ Ccan bound per target), lineage, FAR (conditional on RS),
  diffuse cuts only if supp(z) enters V_U.

## 6. M1 TABLE (founder lock CTX ~15:55: coordinates → exact source → constraint → status → checker)
| idx | coord | exact source (registered object) | rows it enters | status | checker |
|---|---|---|---|---|---|
| 0/1 | S⁻/S⁺ | cell retained load Σ|k|²|u_k|² before/after the event (rg_run1_analyze.py E_m) | S0, I0, BAL | EXACT | rg_c0_m1_check.py |
| 2/3 | A⁻/A⁺ | A_{N,η} reader (frozen record) | A0, BAL | EXACT (reader) | rg_c0_m1_check.py |
| 4 | Gsrc | Σ over targets of live source pairs (rg_m2_targets.json / stepper pair-sum) | ACC0, P0 | EXACT | rg_c0_m1_check.py |
| 5 | Gnet | # targets (or load) with nonzero net forcing | ACC0, ACC1 | EXACT | rg_c0_m1_check.py |
| 6 | Gact | births kept active inside the cell (|t|² < 3, not seed addresses) | ACC1, S0, I0 | EXACT | rg_c0_m1_check.py |
| 7 | Ccan | Gsrc − Gnet (multi-source surplus) | ACC0 | EXACT, not a defect | rg_c0_m1_check.py |
| 8 | R | certified resolutions (T channel) — requires Resolve = PASS | ACC1 | UNRESOLVED in round 0 (set 0; no certifier yet) | — |
| 9 | X | exits |t|² ≥ 3 / active-set boundary | ACC1, ZX, I0 | EXACT | rg_c0_m1_check.py |
| 10 | D | payable defect (declared) | ACC1, ZX | EXACT (0 on the face) | — |
| 11 | Bk | feedback/return onto existing addresses (|t|² = 1 targets) | ACC1, S0, A0, I0 | EXACT | rg_c0_m1_check.py |
| 12 | L | relaxation loss ν·L_R on the invariant | I0 | EXACT (stepper identity ≤ 1e-10) | rg_run1_energy_check.py |
| 13 | p | productivity ≤ Gsrc | P0, NORM | OUTER until Witness Soundness | — |
Inclusion proof status: ExactEvents ⊂ C0 holds by identity for rows ACC0, ACC1, I0 (ledger identities of the sparse
stepper) and by implication for S0, A0, P0; verified on the only fully catalogued exact event (W3 gen-1) by
rg_c0_m1_check.py; to be re-verified on every T0-02 event automatically before that event enters (P_1).

## 7. Toledo / Genesis lookup BEFORE use (founder rule, CTX ~16:00: "ตรวจใน Toledo และ genesis ก่อน ถ้ามีแล้วใช้ของเดิม")
| C0 object | existing object found | how used |
|---|---|---|
| ACC0 Gsrc = Gnet + Ccan | Toledo PROP-P2-CANCELLATION-LEDGER-01 (NS proposals file) ONLY. (CORRECTION: weld/M.46.v1 = additive cancellation of the order (D,≤); EQ-015/P.09.v1 = gauge-anomaly cancellation — keyword hits, NOT this object; struck.) | occurrence — cite, do not re-register |
| I0 energy identity with loss L | Genesis core "spine Lyapunov certificate" (RDL_SpineStability.energy_nonincreasing / strict_decay, machine-checked, core ≈ line 1640); Toledo Lyapunov family EQ-015/S.17–S.22, EQ-015/M.03, CMC/P.16 | occurrence; NOTE: the spine E is a Lyapunov function of the whole spine — the NS-leaf fixture T/D = 20 shows E does not descend per productive step at the leaf, so the dual potential Φ cannot be E alone; cite, do not reuse as Φ |
| occupation / lineage counting over a partition | Genesis core B.3 "occupation normal forms over a partition", D_syn = Π_c(|C_c|+1) [finite_diagnostic]; Toledo EQ-001/C.13.v1, C.14.v1, C.16.v1 (lineage/occupation) | the vertex/type partition of §0 is this object; cite EQ-001/C.16 (lineage) as the parent of ACC1 |
| A (ancestry burden) | Toledo A.8/M.02.v1, A.8/M.21.v1 (ancestry) + frozen-record A_{N,η} | occurrence |
| occupation primal (P_r): z ∈ C^(r), Bz = 0, p(z) = 1 | NOT FOUND (no "occupation flow / circulation LP" object in CANONICAL or proposals; Genesis core has circulation only as the four-force fixture) | NEW → must be registered in Toledo before it is cited as an equation anywhere; until then marked "not yet in Toledo" |
| Farkas dual certificate Bᵀy − ηp ∈ (C^(r))* | NOT FOUND (0 hits "farkas"; Lyapunov hits are the spine energy, different statement) | NEW → same rule |
| RC-OCC-SOUND | NOT FOUND | NEW theorem candidate → vault card OPEN; Toledo registration only when stated as an equation |
Registry health: two proposal files fail to parse (proposals/discrete_epsilon_completion_observability_bridge.json,
proposals/ns_eps18_n1_rowwise_radius.json) — not touched by this session; reported to the founder, not repaired.

## 8. C0 = ASSEMBLY OF EXISTING REGISTERED OBJECTS (founder lock CTX ~16:10; codes verified in CANONICAL.json)
| C0 element | parent (existing) | what the parent already gives | what remains NEW (PROPOSAL only) |
|---|---|---|---|
| admissible order of the whole pipeline | EQ-015/M.04.v1 | Retention→Structure→Translation→Readout→Meaning→Report | — |
| DYN, READ rows (exact translation of an event) | weld/M.02.v1 (domain admissibility: q_D commutes with root dynamics and readout) | q_{D,n+1}∘F_n = F♯∘q_{D,n}, O_{D,n} = O♯∘q_{D,n} | the NS-leaf instance of q_D (event lift α_e) — NS-derived exact, domain use only |
| "future-reader sufficient", future-separating, N/T classification | weld/M.03.v1 (equivalence only relative to declared Q,O,c,L) | z ~_{Q,O,c,L} z′ ⇔ O(F^k z)=O(F^k z′) ∀k≤L | — (RNO Coq file's `reader_sufficient` IS this hypothesis; cite) |
| PASS/OBSTRUCTED/UNRESOLVED per row and per round | weld/E.05.v1 (χ_G ∈ {1,0,⊥}) | status ontology | — |
| Retained-State Sufficiency (G4 side, RS) | weld/E.06.v1 (Suff_{E,L}(Z^cand; Q,O,c,T) ∈ {1,0,⊥} + invariant preservation) | the gate itself | the NS-leaf certificate that Suff = 1 for T0-02 — OPEN |
| REANCHOR / GLUE / cross-cell transport, bridge error ε_j | weld/E.08.v1 (transport compatibility + bridge error) | the compatibility condition and where ε lives | the payable d_e on the reanchor edge — NS-derived; EPSC decay ε_j ≤ Cθ^j — OPEN |
| lineage row ACC1 (no fifth channel) | EQ-001/C.16.v1 (RT-LINEAGE-013) + Genesis lineage preservation gate | lineage identity | ACC1 as a count-ledger identity on the NS leaf — NS-derived exact (checker PASS) |
| ACC0 cancellation ledger | PROP-P2-CANCELLATION-LEDGER-01 | Gsrc = Gnet + Ccan | — |
| I0 energy identity with loss L | spine Lyapunov face (Genesis core, machine-checked) / EQ-015/S.17–S.22 | monotone E on the spine | leaf instance with registered ν-loss — NS-derived exact |
| partition into vertex types | Genesis B.3 occupation normal forms over a partition; EQ-001/C.13–C.16 | D_syn count over a partition | — |
| occupation primal (P_r): z ∈ C^(r), Bz = 0, p(z) = 1 | NONE | — | NEW PROPOSAL (label: NEW DERIVATION / PROPOSAL) |
| Farkas dual certificate Bᵀy − ηp ∈ (C^(r))* | NONE | — | NEW PROPOSAL |
| RC-OCC-SOUND (exact recurrent core ⇒ outer primal feasible) | NONE (conditional on weld/E.06 + M.03 + G4) | — | NEW PROPOSAL (theorem candidate) |
Domain Registration Standard obligations for the NS leaf (Rule Registry, Root DAG, forbidden claims, dual checker,
proof receipt): the vault + C0_SCHEMA + checkers are the draft of these; registration itself is OBSTRUCTED until
the three NEW rows are proposals and the two malformed proposal files are repaired by their owner.
