# TODO v3 — Readout–Clay lane (supersedes v2 ordering; intake stays in TODO_RC_LANE_2026-09-12.md U1–U30)
Founder rule (23:40): proposals arrive as spam; this AI extracts into the system and re-plans by leverage.
CURRENT ATTACK VECTOR (locked, founder map paper/NS_P2_CURRENT_ATTACK_MAP_2026-09-12.md): G4 → G6 → G7 → R_j→0 → FINAL-EQUIV;
inside G6: Type-P (§114 projective) ≠ Type-Φ (phase); A1 third-line (M≠I) → A2 Type-P→channel bridge → A3 identity (M=I)
→ reuse phase packing / fresh-donor → OCSR + WS + recursive cancellation → G4 compactness → G7.

## Closed today (exact, reproducible; all PROPOSAL/finite_diagnostic, none Clay)
- W3 gen-1: no productive zero-novelty extension, any exit convention — Nullstellensatz certificate (checker expands only).
- NPSC depth-1 populated: 0 self-closed productive sets among 26,496 orbit reps (≤8 addresses, box [-2,2]³).
- Structural IMR: self-closed-for-all-amplitudes ⇒ collinear (3-line proof + exhaustive 597,618 sets).
- Type-P transport shell-confined (nullity theorem); identity loops exist (shell-9 rectangle, shell-5 planar).
- GCD ⇒ RNO Coq (axiom-free, conditional); C0_SCHEMA assembled from weld/M.02, M.03, E.05, E.06, E.08, EQ-015/M.04, EQ-001/C.16.
- Founder's local chain (W1–W3 audit … rank-2 self-closure planarity; plane-switch capacity/lineage) verified & merged (#55, #56, #57); 11 + 3 proposals in Toledo (#28, #29, #30 merged).

## RANK 1 — running / next compute (one process)
R1  T0-02 production (A d2 → A d3 → B d2 → B d3 → analyze) → M2 round 1 (P_1 with gen-2 event catalogue; cut H_gen2)
    → A1 on real lineage: third returned lineage := projective class of a retained lineage returning to the loop
    after JOINT reanchor that is not in Fix(M) (definition adopted 23:40).
R2  A3 fixture: shell-9 rectangle web — productivity + zero-defect closure by Gröbner (rg_a3_rectangle_closure.py,
    queued behind T0-02 by a PID waiter). Codes: EMPTY ⇒ identity-monodromy web non-productive (branch B instance) /
    dim ≥ 0 ⇒ branch-B candidate → realization audit.
R3  T0-03 (G4 lane, ν = 1/2000; record frozen) — after R2.

## RANK 2 — registration / governance (agents)
G1  NS PR: Type-P shell confinement + identity-loop fixtures (running).
G2  Toledo: register the two Type-P proposal ids after G1 merges (reuse pipeline; append-only; ACK).
G3  Vault pass 3: cards for U26–U30 (rank-2 planarity, plane-switch, Type-P confinement, three-line rigidity,
    NPSC depth-1 result upgrade to DERIVED finite_diagnostic, attack map card); validator 0 warn; commit private.

## RANK 3 — soundness layer (unchanged)
S1  M3 checker (D_RETAINED / D_ADMISSIBILITY / E_REGISTERED / PATTERN) with D_acc residual rule and U5 rule.
S2  Resolve certifier for the T channel (needed by WS-local general and FRPA/turnover).
S3  Σ-sufficiency perturbation test.

## RANK 4 — open holes with no fixture
H1  ∂₊ diffuse reader ℛ₊ (UBRR).   H2  EPSC decay ε_j ≤ Cθ^j from tape.   H3  A2 Type-P → channel bridge (definition).

## Parked
P1  push of internal repo? (no remote; stays private).  P2  repo rename (unconfirmed).  P3  Volume 7 check.
