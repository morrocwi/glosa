# GLS-2026-009 — Frozen retained starting record + admissible reader family (RG runtime step INPUT -> TYPE+SUFFICIENCY)
# Language: Readout Genesis only. Frozen BEFORE any TRANSFORM/SOLVE run (Maker-Checker: nothing below may be edited after a run starts).
# tier: Dr (input declaration). Founder gate names 2026-09-12 10:08 adopted: Retained-State Sufficiency / Zero-Defect Closure / Defect-to-Claim Amplification.
frozen_record:
  id: T0-2026-09-12-01
  root: "delta_R = (a # b); RD1-RD5; Delta_theta > 0; tau_c > 0"
  grammar_A: "spine at the flow leaf: M = 0, D = nu * L_R (per-mode relaxation), nonlinear generation = quadratic triad coupling B_{p,q}, J = 0, eta = reader residual"
  retained_starting_configuration:
    seed_distinctions: ["p0 = (1,0,0) with polarization T0", "q0 = (2,4,0) with polarization N0", "conjugate images"]
    amplitude_readout: "critical normalization |a_k|^2 = |k|^{-1} (RD units; no SI adapter)"
    everything_else: "null (RD1) -- every further distinction must be GENERATED by grammar A with lineage from these two"
    forbidden: "hand-placed distinctions at higher scale (that would be a datum, not a generated distinction)"
  self_similar_action_of_seed: "S: k -> A k with A^T A = 25 I; scale ratio 5 per step, sqrt(5) per half-step"
reader_family_Pi:
  scale_cells: "cells commuting with S: Lambda_m = 5^{m/2} * lambda_0, chosen so that a source, its co-source and their output sit in three consecutive cells"
  declared_reader_defects:
    epsilon_read: "cell boundaries are a reader choice; declared, not discovered"
    epsilon_dyn: "active-set truncation at depth d is a reader boundary (registered exit), never a physical loss"
  future_influence_retention_reader: "A_{N,eta} := sup_{i<=j} 2^{-2 eta (j-i)} S_i with 0 < eta < beta (relevance-weighted memory of lower cells)"
  hand_off_cost_readouts: ["alpha_sel", "alpha_off", "alpha_cancel", "alpha_total"]  # recent-window generated load at cell m+1 relative to retained load at cell m
  horizon_H: "at least 3 relaxation windows tau_m = c/(nu Lambda_{m+1}^2) of the highest measured cell; c = 1"
  budget_K: "one process; virtual memory <= 3 GB (ulimit -v 3000000); no dense grid above 64^3; active-set depth d in {3,4}; wall-clock <= 90 min; anything unfinished = UNRESOLVED"
  tolerance: "exact rational where possible; float64 only inside the stepper; PASS requires the identity of retained load (energy) to hold to 1e-10 relative"
  lineage_rule: "every generated distinction records its source pair; a distinction without a source path = epsilon_lineage > 0 (report, do not use)"
closure_semantics:
  commuting_square: "q_{m+1} o F = F# o q_m or the defect is declared per cell"
  terminal_classes: "P (kernel-side planar class), D (degenerate/nonproductive)"
  registered_exit: "load crossing the active-set boundary or the top cell = registered exit, reported as such"
failure_codes: [LOST_INFORMATION, INSUFFICIENT_RESOLUTION, NO_CLOSURE, UNRESOLVED]
pass_codes: [PASS_FINITE_DIAGNOSTIC]
question_Q: "under Pi and grammar A, from T0 alone: (i) is A_{N,eta} bounded by a functional of T0 along one coherent lineage? (ii) does each hand-off pay a positive cost alpha_total < 1 - e^{-c}, stable under d = 3 -> 4? (iii) is any generated load recurrent with zero registered defect?"
