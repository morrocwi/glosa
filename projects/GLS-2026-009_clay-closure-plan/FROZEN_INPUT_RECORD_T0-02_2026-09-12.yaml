# GLS-2026-009 — Frozen retained starting record T0-02 (founder ruling 2026-09-12 ~14:00: "T0-02 = W3 datum ตกลง")
# Language: Readout Genesis only. Frozen BEFORE any TRANSFORM/SOLVE run. tier: Dr (input declaration).
# Reason for T0-02: T0-01 lies in terminal class P (2D3C) — its lineage cannot probe NPSC/G6 (R1D readout addendum).
frozen_record:
  id: T0-2026-09-12-02
  root: "delta_R = (a # b); RD1-RD5; Delta_theta > 0; tau_c > 0"           # unchanged
  grammar_A: "unchanged from T0-01 (per-mode relaxation nu * L_R, quadratic triad coupling B_{p,q}, J = 0)"
  retained_starting_configuration:
    seed_distinctions:
      - "k = (0,0,1) with amplitude c = (c1, c2, 0)"
      - "three equal-shell triads i = 0,1,2: p_i = (sqrt3/2 cos th_i, sqrt3/2 sin th_i, 1/2), q_i = k - p_i, th_i in {0, 2pi/3, 4pi/3}"
      - "polarizations a_i = A1_i e_i + A2_i (p_i x e_i), b_i = B1_i e_i + B2_i (q_i x e_i), e_i = (-sin th_i, cos th_i, 0)"
      - "conjugate images (real field: u(-m) = u(m))"
    datum_constraint: "F(k) = 0 at generation 0, i.e. C_0 = C_1 = C_2 =: C with C_i = A2_i B1_i - A1_i B2_i (multi-source cancellation at k)"
    productivity: "C != 0 (else terminal/degenerate class D)"
    amplitude_readout: "one exact rational point on the productive datum variety (dim 12), chosen and RECORDED before the run; second point = the same with all triad phases rotated by pi/3 (phase-B control). Points fixed in rg_t0_02_seed.json before TRANSFORM starts."
    everything_else: "null (RD1) — every further distinction generated by grammar A with lineage from the 14 seed distinctions"
    forbidden: "hand-placed distinctions at higher scale; editing the seed after the first run; choosing the point after seeing any output"
  known_exact_facts_about_this_seed: "gen-1: 82 targets / 68 live / classes |t|^2 in {3/4,1,7/4,9/4,3,13/4,4}; no productive zero-novelty extension under any exit convention (rg_cert_w3_nullstellensatz.json); outward |t|^2 = 3 descendants are registered exits"
  lattice_note: "addresses live in Z[sqrt3]-lattice (not Z^3); the sparse stepper must index by exact address keys; a cubic FFT is NOT available — if the stepper cannot represent the lattice exactly the run returns UNRESOLVED, never an approximation"
reader_family_Pi:
  scale_cells: "Lambda_m = 5^{m/2} * lambda_0 as in T0-01 with lambda_0 chosen so that |k| = 1 and |p_i| = |q_i| = 1 sit in cell 0 and |t|^2 = 3 sits in cell 1 (declare lambda_0 numerically in the seed file)"
  declared_reader_defects: {epsilon_read: "cell boundaries are a reader choice", epsilon_dyn: "active-set truncation at depth d is a registered exit"}
  future_influence_retention_reader: "A_{N,eta} as T0-01, eta = 1/2"
  hand_off_cost_readouts: ["alpha_sel", "alpha_off", "alpha_cancel", "alpha_total", "D_acc (cancellation-accounting residual, NOT the cancellation amount)"]
  turnover_readout: "per generation: births S_r (new energized addresses inside the cell), certified-inert T_r (addresses whose load falls below tolerance AND whose removal changes no declared future readout over one window — else UNRESOLVED, never T), exits E_r, defect D_r; report the balance U A + S = A' + T + E + D residual"
  horizon_H: "at least 3 relaxation windows of the highest measured cell"
  budget_K: "one process; ulimit -v 3000000; no dense grid; active-set depth d in {2,3} first (sqrt3-lattice growth is faster than T0-01); wall-clock <= 90 min per run; unfinished = UNRESOLVED"
  tolerance: "energy identity 1e-10 relative; all classification decisions (birth/inert/exit) at declared threshold 1e-12 of seed load"
  lineage_rule: "as T0-01"
closure_semantics: "as T0-01; terminal classes P and D; registered exits at active-set boundary and top cell"
failure_codes: [LOST_INFORMATION, INSUFFICIENT_RESOLUTION, NO_CLOSURE, UNRESOLVED]
pass_codes: [PASS_FINITE_DIAGNOSTIC]
question_Q:
  i: "is A_{N,eta} bounded by a functional of T0-02 along the lineage, stable d = 2 -> 3?"
  ii: "at each hand-off: alpha_total, and D_acc = 0 to tolerance (cancellation fully accounted)?"
  iii: "balanced turnover: is there any window in which S_r > 0, T_r certified, E_r = D_r = 0 and the active inventory returns (|A_{r+m} - A_r| < tol)? — a YES is a PCR falsifier CANDIDATE (needs realization audit), a NO is finite evidence only"
