# Neighbour table — literature-review scaffold (glosa rule 31d/31e)
# The lit review answers three questions only: which problem we solve · by which method · how the nearest neighbours do it the same or differently. Wording rule (founder 2026-09-04): do not say "we take/reuse/borrow from them" by default — say same / different / cited. EXCEPTION: "adopted from" is written ONLY when the human explicitly instructed to take that thing from that source (a Blackbox Note line or DECISIONS.yaml decision id required — R0 survives only as an internal stage label inside the Blackbox Note, chair ruling A1; it is never public-facing vocabulary). AI-detected similarity is never "adopted".
# Descriptive comparison, no priority/novelty contest. Every row needs a citation_card id (S8) with metadata_verified + claim_match_verified.
# Tier of each row: finite_diagnostic if fetched & verified; Open if not fetched. Never write "no tool does X".

| neighbour | problem they solve | their method | same as ours | different from ours | related work we cite | citation_card | fetched/verified |
|---|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |  |

## Our own row (fill first, so neighbours are compared against something explicit)
- problem (Blackbox Note human words → lens formulation):
- method (spine stage / artifact):
- what would make us wrong (falsifier):
